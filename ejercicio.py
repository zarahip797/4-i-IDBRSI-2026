# Write your MySQL query statement below
class public import public 
def table(self)
trasaction_id = int 
amount = int 
transaction_date = date

def transaction(self):
odd_sum = input(int)
trasaction_date = input(varchar)

public transaction(self):
    trasaction_id = int
    amount = int
    trasaction_date = date 
    return app ({'trasaction_id'}, {'amount'}, {'trasaction_date'})